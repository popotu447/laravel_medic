services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: laravel-app2
        container_name: laravel-app2
        working_dir: /var/www/html
        volumes:
            - ./:/var/www/html
        ports:
            - "8000:8000"
        user: "${UID:-1000}:${GID:-1000}"
        dns:
            - 8.8.8.8
